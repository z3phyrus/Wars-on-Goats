[package]
name = "spacetime_module"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "spacetime_module"
path = "src/main.rs"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
